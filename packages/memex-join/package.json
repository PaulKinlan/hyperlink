{
  "name": "@hyperlink-experiments/memex-join",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "wireit",
    "zip": "wireit"
  },
  "wireit": {
    "manifest": {
      "command": "mkdir -p dist && cp manifest.json dist/",
      "files": [
        "manifest.json"
      ],
      "output": [
        "dist/manifest.json"
      ],
      "clean": "if-file-deleted"
    },
    "html": {
      "command": "mkdir -p dist && cp options.html dist/",
      "files": [
        "options.html"
      ],
      "output": [
        "dist/options.html"
      ],
      "clean": "if-file-deleted"
    },
    "icons": {
      "command": "mkdir -p dist/icons && cp -R icons/* dist/icons/",
      "files": [
        "icons/*.png"
      ],
      "output": [
        "dist/icons/**"
      ],
      "clean": "if-file-deleted"
    },
    "zip": {
      "command": "cd dist && zip -r ../merge.zip .",
      "files": [
        "dist/**"
      ],
      "output": [
        "merge.zip"
      ],
      "dependencies": [
        "build"
      ],
      "clean": "if-file-deleted"
    },
    "build": {
      "command": "esbuild src/background.ts src/create-link.ts src/content.ts src/popup.ts --bundle --platform=browser --outdir=dist --format=esm",
      "files": [
        "src/**/*.ts",
        "manifest.json",
        "popup.html"
      ],
      "output": [
        "dist"
      ],
      "dependencies": [
        "../shared:build",
        "icons",
        "manifest"
      ]
    }
  },
  "dependencies": {
    "@hyperlink-experiments/shared": "1.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.208"
  }
}
