import { OptionsUI } from '@hyperlink-experiments/shared';

// Initialize the options UI with the extension name
const optionsUI = new OptionsUI('summary', 'Summary');

// Initialize on page load
optionsUI.init();
